# Build log — AI usage

Honest record of where AI helped, where it was wrong, and what I changed.
Keep this updated as you build — at the demo you'll be asked to explain
2-3 specific lines an evaluator picks. "The AI wrote it" isn't an answer,
so use this file to actually understand what's here.

## Entries

### [date] — Initial scaffold
- What AI generated: full initial project structure (FastAPI app, models,
  vision/embeddings/guard modules, tests, docs).
- What I verified/changed: _fill in as you review each file — e.g. did you
  change the guard's category-matching heuristic? Adjust thresholds?
  Change the DB schema?_
- What I'd explain if asked about `app/guard.py` lines ~60-75 (the
  category/subject overlap check): _write your own explanation here so
  you're not caught flat-footed at demo time._

### [date] — _next change_
- What AI generated:
- What I verified/changed:
